# DoorDash Drive API Configuration Example
# Copy these to your .env file and Supabase secrets

# ==========================================
# Backend (Supabase Edge Functions)
# Set these as Supabase secrets:
# supabase secrets set DOORDASH_DEVELOPER_ID=xxx
# ==========================================

DOORDASH_DEVELOPER_ID=your_developer_id_here
DOORDASH_KEY_ID=your_key_id_here
DOORDASH_SIGNING_SECRET=your_base64_signing_secret_here
DOORDASH_ENVIRONMENT=sandbox  # Change to 'production' after approval

# ==========================================
# Frontend (.env in project root)
# ==========================================

# Enable DoorDash as a delivery provider option
VITE_DOORDASH_ENABLED=true

# Optional: Enable other providers for comparison
VITE_ROADIE_ENABLED=true
VITE_UBER_ENABLED=false

# ==========================================
# How to Get Credentials
# ==========================================
# 1. Sign up at https://developer.doordash.com/
# 2. Go to Credentials page
# 3. Create new API key
# 4. Save developer_id, key_id, and signing_secret
# 5. Keep signing_secret secure (it's base64-encoded)

# ==========================================
# Production Access
# ==========================================
# Production access is restricted by default.
# Start with sandbox, then apply for production:
# https://developer.doordash.com/en-US/docs/drive/tutorials/get_started/
